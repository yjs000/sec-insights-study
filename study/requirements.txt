# Stage 0~4: sec-insights 백엔드(backend/pyproject.toml)와 동일한 버전대로 고정.
# "왜 여기선 다르게 동작하지?" 같은 버전 문제를 피하기 위한 것이었지만,
# Stage 5부터 NVIDIA NIM(무료)을 쓰기 위해 이후 core를 0.14대로 올렸습니다
# (아래 "Stage 5+" 섹션 참고). 이 버전 불일치 자체도 학습 포인트입니다 —
# study/README.md의 "버전 업그레이드 노트" 참고.
llama-index-core==0.12.23
llama-index-readers-file==0.4.6
llama-index-embeddings-openai==0.3.1
llama-index-llms-openai==0.3.25
llama-index-agent-openai==0.4.6
openai==1.66.2
python-dotenv==1.0.1
# pypdf는 버전을 고정하지 않음 — llama-index-readers-file 0.4.6이 요구하는
# pypdf>=5.1.0,<6.0.0 범위에 pip가 알아서 맞추도록 둡니다.

# Stage 9 (선택)에서만 필요. sec-insights 백엔드(backend/pyproject.toml)와 맞춘 버전.
fastapi==0.115.11
uvicorn==0.22.0
sse-starlette==1.6.1

# ---- Stage 5+ (NVIDIA NIM 전환 이후) ----
# 아래 조합으로 업그레이드하면 위 pin들과 충돌합니다. 새 가상환경을 만들거나
# `pip install -r requirements.txt --upgrade`로 한 번에 맞추세요.
# llama-index-core>=0.13,<0.15
# llama-index-readers-file
# llama-index-embeddings-openai
# llama-index-llms-openai
# llama-index-llms-nvidia
# llama-index-embeddings-nvidia
# openai
# pandas>=2.3.3   # Python 3.14에서 소스 빌드(meson) 실패를 피하려면 wheel 있는 버전 지정 필요
#
# 주의: llama-index-agent-openai(OpenAIAgent)와 llama-index-question-gen-openai는
# core>=0.13을 지원하지 않습니다. Stage 5+는 이 둘을 쓰지 않고
# FunctionAgent(llama_index.core.agent.workflow)와
# LLMQuestionGenerator(llama_index.core.question_gen.llm_generators)로 대체합니다.
